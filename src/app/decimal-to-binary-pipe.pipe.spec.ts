import { DecimalToBinaryPipePipe } from './decimal-to-binary-pipe.pipe';

describe('DecimalToBinaryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalToBinaryPipePipe();
    expect(pipe).toBeTruthy();
  });
});
