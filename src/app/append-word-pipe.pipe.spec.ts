import { AppendWordPipePipe } from './append-word-pipe.pipe';

describe('AppendWordPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AppendWordPipePipe();
    expect(pipe).toBeTruthy();
  });
});
