import { TextWithForbiddenWordsHighlighterDirective } from './text-with-forbidden-words-highlighter.directive';

describe('TextWithForbiddenWordsHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new TextWithForbiddenWordsHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
